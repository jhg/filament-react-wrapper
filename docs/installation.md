# Installation reference

The canonical installation and first-use guide is maintained in the root
[README.md](../README.md). It contains the Composer-only installation, the
optional `--dev` workflow, React-version guidance, first component example,
Filament fields/widgets, asset updates, configuration, and troubleshooting.

Use this page only as a stable link for older references. Advanced API details
are available in [the PHP API reference](api/php.md) and the [configuration
reference](api/config.md).
